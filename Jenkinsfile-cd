#!groovy

node('ansible-master') {
    

    def artServer = Artifactory.newServer url: "$ATF_URL", credentialsId: 'GLOBAL-ARTIFACTORY';
    
    def version;
    def artifactId;
    def groupId;

    stage('download artifacts'){
        // def downloadSpec = """{
        //     "files": [
        //         {
        //             "pattern": "app/com/cfets/devops/ansible-maven-sample/*",
        //             "build": "$build_name/$build_number"
        //         }
        //     ]
        // }"""

        echo "ATF_URL=${params.ATF_URL}";
        echo "version=${params.verison}";
        def downloadSpec = """{
            "files": [
                {
                    "pattern": "app/com/cfets/devops/ansible-maven-sample/${params.version}/ansible-maven-sample-${params.version}-deploy.tar.gz",
                    "flat": true,
                    "target": "."
                }
            ]
        }"""
        artServer.download(downloadSpec);
        sh 'tar -zxvf *.tar.gz'

    }
    
   stage('deploy'){
        ansiblePlaybook colorized: true, credentialsId: 'ansible-slave', extras: '-e app_version="${params.verison}" -e app_user_password="${params.app_user_password}"', inventory: 'ansible-maven-sample-${params.version}/deploy/inventories/stage/hosts', playbook: 'ansible-maven-sample-${params.version}/deploy/app.yml', sudoUser: null
   }

    
    
}
